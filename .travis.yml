language: bash
services: docker

env:
  - VERSION=latest

before_script:
  - env | sort
  - image="artb1sh/postgres-replication:$VERSION"

script:
  - docker build -t "$image" .
  - ./tests/run.sh "$image"

after_script:
  - docker images
